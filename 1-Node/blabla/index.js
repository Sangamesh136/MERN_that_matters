// const { genNum, celTofar } = require("./utils");
// console.log(`random number : ${genNum()}`);
// console.log(`celcius to fahrenhiet: ${celTofar(38)}`);
import { getPosts } from "./postController.js";
console.log(getPosts());
